# NVIDIA-Docker run options
docker_args: -v /home/quan/datasets/private:/data -e PYTHONPATH=/exp

# Run script options
# To run the container multiple times
num_iters: 1
# The name of containers (remember you have to prefix it with '{user}_')
container_name: quan_DLProject_202306_vehicle_cla
# The docker image to use
image_name: quan/dl:cuda11_pytorch18_py38
